ჩვენ გამოვიყენეთ .map() მეთოდი. ეს მეთოდი საუკეთესოა, როცა გვინდა არსებული მასივი გარდავქმნათ ახალ მასივად

sentence.trim() — ეს აშორებს ტექსტს ზედმეტ ცარიელ ადგილებს თავში და ბოლოში
.split(" ") — ეს მეთოდი ეძებს ყველა სფეისს ტექსტში და მის მიხედვით ყოფს სტრინგს მასივად.
.length — აბრუნებს მიღებული მასივის ელემენტების რაოდენობას.
return — .map() მეთოდი ამ 2-იანს სვამს ახალ მასივში.

ეს პროცესი მეორდება ოთხჯერ (რადგან 5 წინადადება გვაქვს) და საბოლოოდ ვიღებთ ახალ მასივს: [3, 2, 1, 4, 5].

sentences (ტექსტების მასივი)
wordCounts (რიცხვების მასივი, რომელიც პირველმა ფუნქციამ დაგვიბრუნა)
მაგალითად, ინდექსზე 0 sentences-ში დევს "ტაიპსკრიპტი კარგია", ხოლო wordCounts-ში იმავე 0 ინდექსზე დევს რიცხვი 2.

1. პირველი ფუნქციის გამოძახება
ფუნქციის დასაწყისშივე ვწერთ: const wordCounts = countWordsInSentences(sentences);. ამით ჩვენს ახალ ფუნქციას "ვაწვდით" ინფორმაციას იმის შესახებ, თუ რომელ წინადადებაში რამდენი სიტყვაა.

2. .filter() მეთოდი გადაუვლის საწყის მასივს და ტოვებს მხოლოდ იმ ელემენტებს, რომლებიც აკმაყოფილებენ ჩვენს პირობას (სადაც return true მოხდება).

ჩვეულებრივ, .filter((sentence) => ...) იღებს მხოლოდ ელემენტს, მაგრამ ჩვენ აქ გამოვიყენეთ მისი მეორე პარამეტრიც — ინდექსი (i):
sentences.filter((sentence, i) => { ... })

ეს i გვეხმარება გავიგოთ, მერამდენი წინადადება მოწმდება მიმდინარე მომენტში. თუ ვამოწმებთ მე-2 წინადადებას 

3. if / else ლოგიკა
თუ isLong არის true, ვამოწმებთ: currentWordCount >= limit. თუ ეს პირობა სრულდება, წინადადება ვარდება ახალ მასივში.
თუ isLong არის false (ანუ else ბლოკი), ვამოწმებთ: currentWordCount < limit. თუ ნაკლებია, მაშინ რჩება.
