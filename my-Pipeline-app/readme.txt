======================================================================
     კონსპექტი: processNumbers ფუნქციის მუშაობის პრინციპი (TypeScript)
======================================================================
----------------------------------------------------------------------
ნაწილი 1: ფუნქციის დეკლარაცია და ტიპები (Function Signature)
----------------------------------------------------------------------
კოდი:
function processNumbers(
    nums: number[], 
    filterFn: (num: number) => boolean, 
    transformFn: (num: number) => number
): number[]
განმარტება:
* function processNumbers -> ჩვენი მთავარი ფუნქციის სახელი.
* nums: number[] -> პირველი არგუმენტი. ნიშნავს, რომ ფუნქციას უნდა გადავცეთ 
  რიცხვების მასივი (მაგალითად: [1, 2, 3]).
* filterFn: (num: number) => boolean -> მეორე არგუმენტი, რომელიც არის ფუნქცია (Callback).
  ის სათითაოდ აიღებს თითოეულ რიცხვს და დააბრუნებს ბულიანს (true ან false). 
  მისი საქმეა გადაწყვიტოს: "დავტოვოთ ეს რიცხვი თუ გადავაგდოთ?".
* transformFn: (num: number) => number -> მესამე არგუმენტი, რომელიც ასევე ფუნქციაა.
  ის იღებს რიცხვს და აბრუნებს სახეცვლილ რიცხვს. მისი საქმეა რიცხვის შეცვლა 
  (მაგალითად: გაორმაგება, კვადრატში აყვანა და ა.შ.).
* ბოლოში მითითებული ": number[]" -> ნიშნავს, რომ თავად processNumbers ფუნქციაც, 
  მუშაობის დასრულების შემდეგ, დააბრუნებს ახალ, გადამუშავებულ რიცხვების მასივს.
----------------------------------------------------------------------
ნაწილი 2: ფუნქციის შიდა ლოგიკა (Function Body)
----------------------------------------------------------------------
კოდი:
const result: number[] = [];
განმარტება:
* აქ იქმნება ახალი, სრულიად ცარიელი მასივი, რომელსაც ქვია 'result'. 
* აქ ჩაიწერება მხოლოდ ის რიცხვები, რომლებიც ფილტრს გაივლიან და ტრანსფორმირდებიან.

კოდი:
for (let i = 0; i < nums.length; i++) {
    const currentNumber = nums[i];
განმარტება:
* 'for' ციკლით სათითაოდ მივყვებით (ავუყვებით) საწყის 'nums' მასივს პირველი ელემენტიდან ბოლო ელემენტამდე.
* 'currentNumber' არის ცვლადი, რომელშიც ყოველ ბიჯზე ინახება ის

კოდი:
if (filterFn(currentNumber)) {
განმარტება:
* აქ ხდება მიმდინარე რიცხვის შემოწმება. ჩვენ ვიძახებთ 'filterFn' ფუნქციას და გადავცემთ 'currentNumber'-ს.
* თუ 'filterFn'-მა დააბრუნა 'true', კოდი შედის 'if' ბლოკის შიგნით (ანუ რიცხვმა გაიარა ფილტრი).
* თუ დააბრუნა 'false', კოდი ამ რიცხვს აიგნორებს და გადადის შემდეგზე.

კოდი:
    const transformedNumber = transformFn(currentNumber);
    result.push(transformedNumber);
}
განმარტება:
* თუ კოდი შევიდა 'if'-ში, ამ რიცხვს ვაწვდით 'transformFn' ფუნქციას, რომელიც მას სახეს უცვლის.
* მიღებულ ახალ რიცხვს ('transformedNumber') ინსტრუმენტ 'push()'-ის დახმარებით ვამატებთ ჩვენს ცარიელ 'result' მასივში.


კოდი:
return result;
განმარტება:
* როდესაც ციკლი მთლიან მასივს მორჩება, ფუნქცია 'return'-ის საშუალებით გვიბრუნებს ახალ შევსებულ მასივს.
----------------------------------------------------------------------
ნაწილი 3: გამოყენების პრაქტიკული მაგალითი (Execution)
----------------------------------------------------------------------
კოდი:
const MyNumbers = [1, 2, 3, 4, 5];

const result1 = processNumbers(
  MyNumbers, 
  (n) => n % 2 === 0, 
  (n) => n * 2        
);
როგორ მუშაობს ეს კონკრეტული გამოძახება ნაბიჯ-ნაბიჯ:
1. ციკლი იწყებს მუშაობას [1, 2, 3, 4, 5] მასივზე.
2. რიცხვი 1: ფილტრი (1 % 2 === 0) აბრუნებს false-ს. გამოიტოვება.
3. რიცხვი 2: ფილტრი (2 % 2 === 0) აბრუნებს true-ს. გადადის ტრანსფორმაციაზე: 2 * 2 = 4. იწერება result-ში.
4. რიცხვი 3: ფილტრი აბრუნებს false-ს. გამოიტოვება.
5. რიცხვი 4: ფილტრი აბრუნებს true-ს. ტრანსფორმაცია: 4 * 2 = 8. იწერება result-ში.
6. რიცხვი 5: ფილტრი აბრუნებს false-ს. გამოიტოვება.
7. ბოლო შედეგი: [4, 8].
======================================================================